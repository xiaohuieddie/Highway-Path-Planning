// Will be useful if we are using cost function approach
#include <map>
#include <string>
#include <vector>

using std::map;
using std::string;
using std::vector;

struct Vehicle{
  double x;
  double y;
  double vx;
  double vy;
  double s;
  double d;
};